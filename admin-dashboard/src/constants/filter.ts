export enum SORT_BY {
  ID_ORDER = 'idOrder',
  STATUS = 'status',
  RELEASE_DATE = 'createdAt',
  DEADLINE = 'deadline',
  PRICE = 'price',
}

export enum SORT_ORDER {
  ASC = 'asc',
  DESC = 'desc',
}
